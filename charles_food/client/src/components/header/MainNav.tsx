import { Button } from "..";

const MainNav = () => {
  return (
    <Button
      className="font-bold hover:text-orange-500 hover:bg-white"
      variant="ghost"
    >
      Log In
    </Button>
  );
};

export default MainNav;
